doctype html
html
    head
        title Welcome to Yatri.com
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        style.
            body { font-family: Arial, sans-serif; text-align: center; margin-top: 50px; }
            .btn { margin: 10px; padding: 10px 20px; font-size: 16px; cursor: pointer; border: none; border-radius: 4px; }
            .new-user { background-color: #4CAF50; color: white; }
            .signin { background-color: #008CBA; color: white; }
    body
        h1 Welcome to Yatri.com
        div
            button.btn.new-user(onclick="location.href='/admin/register'") New User
            button.btn.signin(onclick="location.href='/signin'") Sign In